<?xml version="1.0" encoding="UTF-8"?>

<configuration>
	<include resource="org/springframework/boot/logging/logback/base.xml" />

	<!-- 测试环境，开发环境. -->
	<springProfile name="test,dev">
		<logger name="org.springframework.web" level="INFO" />
		<logger name="com.weixin.validation.app.listener" level="INFO" />
		<logger name="com.weixin.gitcommand.dao" level="INFO" />
	</springProfile>

	<!-- 生产环境. 主要记录Service层在做数据库交互的时候产生的错误 -->
	<!-- logger 指定appender来输出，additivity为false表示不向上级传递 -->
	<springProfile name="prod">
		<logger name="org.springframework.web" level="ERROR" additivity="false">
			<appender-ref ref="drFile"/>
		</logger>
		<logger name="com.weixin.validation.app.service" level="ERROR" additivity="false" >
			<appender-ref ref="drFile"/>
		</logger>
	</springProfile>

	<appender name="drFile"
		class="ch.qos.logback.core.rolling.RollingFileAppender">
		<File>/var/jlog/</File>
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">    
			<!-- daily rollover -->
			<FileNamePattern>git-wx.%d{yyyy-MM-dd}.log</FileNamePattern>    
			<!-- keep 30 days' worth of history -->
			<maxHistory>30</maxHistory>
		</rollingPolicy>
		<encoder>
			<Pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{35} - %msg %n</Pattern>
		</encoder>
	</appender>

</configuration>